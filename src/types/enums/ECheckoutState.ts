/**
 * Based on: https://optimize-docs.billwerk.com/docs/checkoutstate-enum
 */
export enum ECheckoutState {
    INIT = "Init",
    OPEN = "Open",
    ACCEPT = "Accept",
    CANCEL = "Cancel",
    CLOSE = "Close",
    ERROR = "Error",
}
